<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zh.myfilter.dao">

    <select id="findAll" resultType="com.zh.myfilter.entity.UserEntity">
        select pass from stu where name = #{name}
    </select>

    <update id="">
        UPDATE ear_mark_batch ,ear_mark_store
        set ear_mark_batch.used=
        (select count(1) from ear_mark_store where ear_mark_batch.id=ear_mark_store.batch_id),
        ear_mark_batch.unused=quantity-used
        where ear_mark_batch.farm_id = ear_mark_store.farm_id
        and ear_mark_batch.farm_id=#{farmId}
    </update>


</mapper>